{
  "name": "hello_node",
  "version": "1.0.1",
  "description": "Hello World from Node API",
  "author": "William Franco",
  "license": "MIT",
  "main": "app.js",
  "scripts": {
    "start": "node ./dist/server.js",
    "dev": "npx nodemon src/server.ts --watch 'src/' -e ts",
    "compile": "npm install && npx tsc",
    "clean": "rm -rf node_modules && rm -rf",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "nodemon": "^2.0.22"
  },
  "devDependencies": {
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.17",
    "@types/nodemon": "^1.19.2",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.3"
  }
}